<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.obnoxious.ecatering">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.RegisterActivity"></activity>
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.CartActivity" />
        <activity android:name=".ui.SplashScreen" />
        <activity
            android:name=".view.EventActivity"
            android:label="@string/app_name" />
        <activity android:name=".ui.WelcomeActivity" />
        <activity android:name=".ui.NoConnectionActivity" />
        <activity android:name=".view.EventTimeActivity" />
        <activity android:name=".view.PackageActivity" />
        <activity android:name=".view.PackageDetailsActivity" />
    </application>

</manifest>